import numoop

class TestODE:
    def test_init(self) -> None:
        # Init without anything
        def test_fun(i,j):
            return i+j
        #midp = numoop.Midpoint_Scal(2.)
        #midp.set_func(test_fun)
        eul.ode()
